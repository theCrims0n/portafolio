import { StaticImageData } from "next/image";

export interface Experience {
    id: string;
    job: string;
    title: string;
    description: string;
    footer: string;
    image: string;
    date : string;
}
